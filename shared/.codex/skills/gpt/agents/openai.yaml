interface:
  display_name: "GPT"
  short_description: "GPT-oriented SDK prompting"
  default_prompt: "Use $gpt to work on KitFlow SDK tasks in the shared module with GPT-specific prompting and reasoning."

policy:
  allow_implicit_invocation: true

